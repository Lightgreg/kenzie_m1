comando para usar o GIT


git init = iniciar o git
git status = verificar quais foram alterados ou qualquer outra informaçao
git add "" = colocar arquivos na mesa para depois mandar para o github
git commit -m "" = adicionar comentario que vai aparecer no github
git config --global user.name "" = adicionar nome para a pessoa.
git config --global user.email "" = adicionar email

criando a chave

eval "$(ssh-agent -s)"
ssh-add ~/.ssh/id_ed25519
clip < ~/.ssh/id_ed25519.pub = depois disso colar a chave no github
